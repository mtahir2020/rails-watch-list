<h1>Movie Lists</h1>

<ul>
  <% @lists.each do |list| %>
  <li> <%= link_to list.name, list_path(list) %> </li>
  <% end %>
</ul>

<h2>Add a list</h2>

<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>
